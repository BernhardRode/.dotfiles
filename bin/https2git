#!/bin/bash

REGEX="s/url = https\:\/\/github.com\//url = git@github.com:/g"

# i want to check if there is an .git/config file and replace the https with git url there

if [ -f ".git/config" ]; then
    echo "Replacing https with git url in .git/config"
    sed -i '' -e "$REGEX" .git/config
fi
